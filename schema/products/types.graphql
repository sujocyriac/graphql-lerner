enum ProductType {
  ELECTRONICS
  BOOKS
  CLOTHING
  HOME
  BEAUTY
}

type StoreLocation {
  place: String
  country: String
  lattitude: Int
  longitude: Int
}

type Product {
  id: ID!
  name: String
  description: String
  price: Float
  inStock: Boolean
  type: ProductType
  storeLocations: [StoreLocation]
}

type Query {
  user: String
  getProduct(id: ID!): Product
}

input StoreLocationInput {
  place: String
  country: String
  lattitude: Int
  longitude: Int
}


input ProductInput {
  name: String!
  description: String
  price: Float!
  inStock: Boolean!
  type: ProductType!
  storeLocations: [StoreLocationInput]
}

type Mutation {
  addProducts(input: ProductInput): ID
}
